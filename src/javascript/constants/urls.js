const WEBSITE_URL = MyPluginData.rest_url

const plugin_url_part = "vue-plugin/v1"

export const URLS = {
    base_plugin : `${WEBSITE_URL}${plugin_url_part}`,
    media_url : `${WEBSITE_URL}wp/v2/media`,
    connection : `${WEBSITE_URL}jwt-auth/v1/token`,
}